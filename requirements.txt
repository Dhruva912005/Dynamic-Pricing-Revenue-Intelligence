# Streamlit Cloud — Dynamic Pricing & Revenue Intelligence
# Python 3.11 (see runtime.txt)
# All versions pinned for reproducible, stable cloud deployment.
# Root-cause fix: preprocessing_pipeline.pkl is no longer loaded directly.
# The app reconstructs the ColumnTransformer from project data if pkl fails.
# XGBoost model is loaded from demand_model.json (native format, version-agnostic).

streamlit>=1.32.0
pandas>=2.0.0,<3.0.0
numpy==1.26.4
plotly>=5.24.0
scikit-learn>=1.4.0,<2.0.0
xgboost>=2.0.0,<3.0.0
statsmodels>=0.14.0
joblib>=1.3.0
scipy>=1.10.0,<1.14.0
